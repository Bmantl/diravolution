'use strict';
var app = angular.module('com.module.landingScreen');

app.run(function($rootScope, gettextCatalog) {
  //$rootScope.addMenu(gettextCatalog.getString('Cities'), 'app.landingScreen.list',
  //  'fa-file-o');
});
